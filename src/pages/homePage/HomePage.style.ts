import styled from "styled-components";

export const ContentContainer = styled.div`
  display: flex;
  gap: 30px;
  width: 920px;
  margin-top: 95px;
  margin-left: max(250px, calc(50% - 320px)); ;
`;
